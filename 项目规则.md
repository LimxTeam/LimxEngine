# Limx Engine 项目开发规则

**版本**: 1.0.0  
**生效日期**: 2025-12-05  
**状态**: 强制执行

---

## 一、核心约束（不可违反）

### 1.1 技术栈约束

| 约束项 | 要求 | 说明 |
|--------|------|------|
| **C++ 标准** | C++23 | 使用最新语言特性 |
| **标准库** | **禁止使用** | 不允许 `#include <vector>` 等任何 STL |
| **编译器** | MSVC 19.38+ | Visual Studio 2022 17.8+ |
| **图形 API** | Vulkan 1.4.321.1 | 不支持其他图形 API |
| **平台** | Windows x64 | 暂不支持其他平台 |

### 1.2 禁止行为

| 禁止项 | 说明 |
|--------|------|
| **禁止简化代码** | 不得为快速完成任务而简化实现 |
| **禁止临时禁用** | 不得为快速修复报错而临时禁用功能 |
| **禁止敷衍方案** | 不得使用"先这样后面再改"的临时方案 |
| **禁止忽略警告** | 所有编译警告必须解决，不得使用 `#pragma warning(disable)` |
| **禁止未验证假设** | 所有 API 调用前必须验证其存在性和正确性 |

---

## 二、代码质量标准

### 2.1 文件头注释规范

每个源文件必须包含以下格式的头注释：

```cpp
/*******************************************************************************
 * 文件: [文件名]
 * 创建时间: 2025-12-DD
 * 创建者: AspectTeam
 *
 * 功能描述:
 *   [详细描述文件的核心功能和技术特点]
 *   [算法复杂度分析]
 *   [空间-时间权衡说明]
 *
 * 设计哲学:
 *   [阐述设计决策和架构考虑]
 *
 * 技术特性:
 *   - 特性1: [具体说明]
 *   - 特性2: [具体说明]
 *
 * 依赖关系:
 *   [内部依赖]
 *   [外部依赖]
 *
 * 注意事项:
 *   [使用限制、平台依赖、性能注意事项等]
 *
 ******************************************************************************/
```

### 2.2 命名规范

| 类型 | 规范 | 示例 |
|------|------|------|
| 类/结构体 | PascalCase | `BlockAllocator`, `RenderGraph` |
| 接口 | I + PascalCase | `IAllocator`, `IPlugin` |
| 枚举类型 | PascalCase | `TextureFormat`, `QueueType` |
| 枚举值 | PascalCase | `RGBA8Unorm`, `Graphics` |
| 函数/方法 | PascalCase | `Allocate()`, `CreateBuffer()` |
| 模板参数 | T + Name | `TElement`, `TAllocator` |
| 常量 | k + PascalCase | `kMaxSize`, `kDefaultAlignment` |
| 私有成员 | m_ + PascalCase | `m_Capacity`, `m_Data` |
| 静态成员 | s_ + PascalCase | `s_Instance`, `s_Counter` |
| 全局变量 | g_ + PascalCase | `g_Allocator`, `g_Device` |
| 局部变量 | camelCase | `bufferSize`, `elementCount` |
| 命名空间 | PascalCase | `Limx::Memory`, `Limx::RHI` |

### 2.3 代码风格

| 规则 | 要求 |
|------|------|
| **缩进** | 4 空格，禁止 Tab |
| **行宽** | 最大 100 字符 |
| **大括号** | Allman 风格（独占一行） |
| **空格** | 操作符两侧加空格 |
| **空行** | 逻辑块之间用空行分隔 |

示例：
```cpp
namespace Limx
{
    class BlockAllocator : public IAllocator
    {
    public:
        void* Allocate(SizeType size, SizeType alignment) override;
        void Deallocate(void* ptr) override;

    private:
        void* m_Memory;
        SizeType m_Capacity;
    };
}
```

---

## 三、开发流程规范

### 3.1 开发前必须确认

在编写任何代码之前，必须：

1. **说明设计方案** - 描述要实现什么、如何实现
2. **等待确认** - 用户同意后才能开始编码
3. **分步实现** - 复杂功能分多步完成，每步确认

### 3.2 代码实现检查清单

每次提交代码前必须确认：

- [ ] 无编译错误
- [ ] 无编译警告
- [ ] 无标准库依赖
- [ ] 符合命名规范
- [ ] 包含完整文件头注释
- [ ] 所有公共 API 有注释
- [ ] 边界条件已处理
- [ ] 资源管理正确（RAII）

### 3.3 禁止的快捷方式

| 禁止做法 | 正确做法 |
|----------|----------|
| `// TODO: 后面实现` 然后留空函数 | 要么完整实现，要么不创建 |
| 注释掉报错代码 | 找到根本原因并修复 |
| 使用 `#if 0` 禁用代码块 | 删除或正确实现 |
| 返回硬编码值绕过逻辑 | 实现完整逻辑 |
| 捕获异常后忽略 | 正确处理或向上传播 |

---

## 四、架构设计原则

### 4.1 核心原则

| 原则 | 说明 |
|------|------|
| **零开销抽象** | 抽象不应引入运行时开销 |
| **数据导向设计** | 优先考虑数据布局和缓存效率 |
| **编译时计算** | 能在编译时完成的不留到运行时 |
| **显式优于隐式** | 避免隐式转换和隐藏行为 |
| **RAII** | 资源获取即初始化，析构即释放 |

### 4.2 内存管理原则

| 原则 | 说明 |
|------|------|
| **无裸 new/delete** | 所有分配通过分配器 |
| **所有权明确** | 每个资源有且仅有一个所有者 |
| **生命周期清晰** | 资源创建和销毁路径明确 |

---

## 五、反思与验证机制

### 5.1 多角度审查

每个重要实现必须从以下角度审查：

1. **架构师视角** - 设计是否简洁、可扩展？
2. **性能工程师视角** - 是否有性能瓶颈？内存访问模式是否高效？
3. **安全专家视角** - 是否有越界、泄漏、竞态？
4. **维护者视角** - 代码是否易于理解和修改？

### 5.2 创新要求

每个模块实现应至少包含一项：

- 算法改进（复杂度优化）
- 内存优化（空间效率）
- 并行化策略
- 缓存友好设计

---

## 六、交互协议

### 6.1 AI 必须遵守

1. **先计划后执行** - 复杂任务先说明计划，等待确认
2. **不擅自简化** - 遇到困难时询问，不自行降低标准
3. **完整实现** - 要么完整实现，要么明确说明无法完成
4. **主动报告问题** - 发现设计问题主动提出

### 6.2 沟通格式

**提出方案时**：
```
## 设计方案

### 目标
[要实现什么]

### 方案
[如何实现]

### 涉及文件
[将创建/修改哪些文件]

### 是否同意此方案？
```

**遇到问题时**：
```
## 遇到问题

### 问题描述
[具体问题]

### 可能原因
[分析]

### 建议方案
[解决思路]

### 需要确认
[需要用户决定的事项]
```

---

## 七、质量标准

### 7.1 编译标准

- 零错误
- 零警告（/W4 /WX）
- 静态分析通过

**此规则文档具有最高优先级，任何开发行为必须遵守。**
