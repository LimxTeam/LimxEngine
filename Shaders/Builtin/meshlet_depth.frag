#version 450

// ============================================================================
// meshlet_depth.frag — 网格着色器深度路径的片段着色器
//
// 空的。深度写入由固定功能做, 片段着色器什么也不用输出。
//
// 那为什么还要有它: Vulkan 的图形管线在有颜色附件或需要深度写入时必须有
// 片段阶段 (除非用 rasterizerDiscardEnable)。没有它的话管线创建失败, 而
// 那条错误信息不会说"你少了一个空的片段着色器"。
//
// 也**不做 alpha 测试**。这是一个有意的取舍, 不是遗漏: meshlet 剔除只收
// 不透明批次, 蒙版材质要到材质解析那一天才有地方放。判据据此成立 ——
// 本路径画的三角形集合是经典深度预通道的**子集**, 于是每个像素上本路径
// 的深度只能等于或者远于经典路径的。
// ============================================================================

void main()
{
}
